spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/auth_db
    username: ${DB_USERNAME:isms_admin}
    password: ${DB_PASSWORD:postgresPwd2025}
    hikari:
      maximum-pool-size: 5
      minimum-idle: 2

  jpa:
    show-sql: true                # SQL in console when developing